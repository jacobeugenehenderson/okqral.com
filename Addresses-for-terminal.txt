Local:

cd "/Users/jacobhenderson/Desktop/okQRal/public"
python3 -m http.server 5500
http://localhost:5500/index.html



Push to git:
cd "/Users/jacobhenderson/Desktop/okQRal"

# 1) Stage & commit your changes on main
git add -A && git commit -m "Deploy: UI fixes, copy tweaks (2025-10-22 AM)" && git push

# 2) Tag (optional but recommended)
git tag -a v$(date +%Y.%m.%d)-AM -m "Morning deploy" && git push --tags

# 3) Deploy ONLY /public to gh-pages (the live site)
git subtree push --prefix public origin gh-pages

  // Disable SHAPE + BODY when Emoji is selected

